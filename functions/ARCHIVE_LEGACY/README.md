# Legacy Firebase Functions Source (Archived)

هذه الملفات نُقلت من المسار `firebase/functions/src` بعد توحيد الكود في `functions/src`.

Purpose:
- Preserve old implementations temporarily for diff/reference.
- Should NOT be deployed.

Next Steps:
1. Confirm no unique logic is only present here.
2. Migrate anything missing (if discovered) to new consolidated tree.
3. Delete this directory when confident.

Do not modify these files further; treat them as read-only for audit.
